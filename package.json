{
  "name": "osrm-frontend",
  "version": "0.4.0",
  "description": "frontend interface for project osrm",
  "main": "src/index.js",
  "scripts": {
    "test": "jest",
    "test:lint": "eslint src/*.js i18n/*.js",
    "replace": "node ./scripts/replace.js",
    "compile": "browserify -d src/index.js -s smap > bundle.raw.js && uglifyjs bundle.raw.js --compress --mangle --source-map filename=bundle.js.map -o bundle.js",
    "esbuild": "esbuild bundle.js --bundle --allow-overwrite --minify --minify-syntax --tree-shaking=true --format=iife --platform=browser --legal-comments=none --charset=utf8 --drop:debugger --drop:console --ignore-annotations --log-level=error --outfile=bundle.esbuild.js --sourcemap=external",
    "obfuscate": "javascript-obfuscator bundle.esbuild.js --output bundle.min.js --compact true --control-flow-flattening true --dead-code-injection true --debug-protection true --disable-console-output true --identifier-names-generator hexadecimal --numbers-to-expressions true --self-defending true --string-array true --string-array-encoding base64 --string-array-wrappers-count 2 --string-array-wrappers-chained-calls true --string-array-wrappers-type function --unicode-escape-sequence true",
    "clear": "rm -rf bundle.js bundle.js.map bundle.raw.js bundle.esbuild.js bundle.esbuild.js.map bundle.min.js.map",
    "build": "npm run clear && npm run replace && npm run compile && npm run esbuild && npm run obfuscate && npm run clear && cp node_modules/leaflet/dist/leaflet.css css/leaflet.css",
    "start-index": "budo src/index.js --port=9966 --host=0.0.0.0 --serve=bundle.min.js -d | bistre",
    "start-index-prod": "budo src/index.js --port=9966 --host=0.0.0.0 --serve=bundle.min.js -d | bistre",
    "start": "npm run build && npm run start-index",
    "start-prod": "npm run build && npm run start-index-prod",
    "prepub": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "osrm-frontend"
  },
  "keywords": [
    "osrm",
    "lrm",
    "directions",
    "routing",
    "api"
  ],
  "author": "amy lee walton",
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/test/**/*.test.js"
    ]
  },
  "license": "ISC",
  "devDependencies": {
    "bistre": "^1.0.1",
    "browserify": "^17.0.1",
    "budo": "^11.8.4",
    "jest": "^30.3.0",
    "jest-environment-jsdom": "^30.3.0",
    "uglify-js": "^3.19.3"
  },
  "dependencies": {
    "esbuild": "^0.28.0",
    "file-saver": "^2.0.5",
    "javascript-obfuscator": "^5.4.1",
    "jsonp": "^0.2.1",
    "leaflet": "~1.9.4",
    "leaflet-control-geocoder": "^3.3.1",
    "leaflet-routing-machine": "3.2.12",
    "leaflet.locatecontrol": "^0.89.0",
    "local-storage": "^2.0.0",
    "osrm-text-instructions": "^0.15.0",
    "qs": "^6.15.0"
  }
}
